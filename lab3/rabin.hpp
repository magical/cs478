#pragma once
#include <string>
#include <vector>

std::vector<std::string> dispersal(const std::vector<std::string> &input, int t);
std::vector<std::string> recover(const std::vector<std::string> &input, std::vector<int> indices);
